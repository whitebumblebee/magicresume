rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Session snapshots are server-only. Browser clients use authenticated
    // Next.js routes and never receive Firestore credentials.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
